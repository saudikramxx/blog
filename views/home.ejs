<%- include('PARTIALS/header'); -%>
  <h1>HOME</h1>
  <p><%=content%></p>
  
  <% posts.forEach(function(post) {%>
    <h1><%= post.title %></h1> 
    <p><%= post.content.substring(0,100) + "..."%></p>
      <a href="/post/+<%= post.title %>">readmore</a>
    
  <% }) %>
  <%- include('PARTIALS/footer'); -%>